#[derive(Debug, PartialEq, Clone)]
pub enum TokenType {
    Ident(String),
    Int(i32),
    StringLiteral(String),
    Equals,
    Mut,
    Extrn,
    Comma,
    Include,
    Ellipsis,
    OpenCurly,
    CloseCurly,
    OpenParen,
    CloseParen,
    Semicolon,
    Fn,
    Let,
    Return,
    Plus,
    Minus,
    Star,
    Slash,
    Eof,
}
